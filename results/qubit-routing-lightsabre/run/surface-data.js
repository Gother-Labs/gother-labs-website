window.QUBIT_ROUTING_SURFACE_DATA = {
  "meta": {
    "baseline_added_cnot": 308076,
    "accepted_added_cnot": 295782,
    "added_cnot_reduction": 12294,
    "baseline_weighted_cnot_delta": 8938.8,
    "accepted_weighted_cnot_delta": 11506.2,
    "weighted_cnot_delta_gain": 2567.4,
    "total_cases": 72,
    "accepted_candidate_id": "gen-62-i0-w3-a0-8185f10d0dff4245"
  },
  "code_snapshots": [
    {
      "label": "baseline policy",
      "lines": [
        "struct FrontLayerScores {",
        "    pairs: Vec<[usize; 2]>,",
        "}",
        "",
        "impl FrontLayerScores {",
        "    fn from_ctx(ctx: &SwapSelectionContext<'_>) -> Self {",
        "        Self { pairs: ctx.front_layer().physical_pairs() }",
        "    }",
        "",
        "    fn score_delta(&self, topology: &Topology, swap: Swap) -> f64 {",
        "        self.pairs.iter()",
        "            .map(|pair| distance_delta(pair, topology, swap))",
        "            .sum()",
        "    }",
        "}",
        "",
        "pub struct CandidatePolicy {",
        "    front_weight: f64,",
        "}",
        "",
        "impl CandidatePolicy {",
        "    pub fn choose_swap(&self, ctx: &SwapSelectionContext<'_>) -> Option<Swap> {",
        "        ctx.candidate_swaps()",
        "            .min_by(|a, b| self.score_swap(ctx, *a).total_cmp(",
        "                &self.score_swap(ctx, *b),",
        "            ))",
        "    }",
        "",
        "    fn score_swap(&self, ctx: &SwapSelectionContext<'_>, swap: Swap) -> f64 {",
        "        let topology = ctx.topology();",
        "        let front = FrontLayerScores::from_ctx(ctx);",
        "        self.front_weight * front.score_delta(topology, swap)",
        "    }",
        "}"
      ]
    },
    {
      "label": "step 1 · gen 1 · 9,541.2",
      "candidate_id": "gen-1-i0-w0-a0-b261d6caca74d84b",
      "candidate_index": 1,
      "lines": [
        "pub struct CandidatePolicy {",
        "    pub basic_weight: f64,",
        "    pub lookahead_weight: f64,",
        "    pub lookahead_size: usize,",
        "    pub set_scaling: SetScaling,",
        "    pub use_decay: bool,",
        "    pub decay_increment: f64,",
        "    pub decay_reset: usize,",
        "    pub best_epsilon: f64,",
        "    decay_state: Vec<f64>,",
        "}",
        "",
        "impl Default for CandidatePolicy {",
        "    fn default() -> Self {",
        "        // Tuned for better CNOT reduction on routing-portfolio shapes.",
        "        // Increased lookahead size and adjusted weights for better performance.",
        "        Self {",
        "            basic_weight: 1.2,",
        "            lookahead_weight: 1.0,",
        "            lookahead_size: 30,",
        "            set_scaling: SetScaling::Size,",
        "            use_decay: false,",
        "            decay_increment: 0.001,",
        "            decay_reset: 5,",
        "            best_epsilon: 1e-10,",
        "            decay_state: Vec::new(),",
        "        }",
        "    }",
        "}",
        "",
        "impl CandidatePolicy {",
        "    fn refresh_decay_state(&mut self, ctx: &SwapSelectionContext<'_>) {",
        "        if !self.use_decay {",
        "            return;",
        "        }",
        "",
        "        let num_qubits = ctx.topology().num_qubits();",
        "        if self.decay_state.len() != num_qubits {",
        "            self.decay_state = vec![1.0; num_qubits];",
        "        }",
        "",
        "        if ctx.swaps_since_progress() == 0 {",
        "            self.decay_state.fill(1.0);",
        "            return;",
        "        }",
        "",
        "        let Some((a, b)) = ctx.last_applied_swap() else {",
        "            return;",
        "",
        "        &mut self,",
        "        ctx: &InitialLayoutContext<'_> ,",
        "        _rng: &mut RngState,",
        "    ) -> Result<Vec<usize>, RouterError> {",
        "        self.decay_state.clear();",
        "        choose_disjoint_aware_layout(ctx)",
        "    }",
        "",
        "    fn choose_best_swap(",
        "        &mut self,",
        "        ctx: &SwapSelectionContext<'_> ,",
        "        rng: &mut RngState,",
        "    ) -> Option<(usize, usize)> {",
        "        self.refresh_decay_state(ctx);",
        "",
        "        let front_layer = FrontLayerScores::from_ctx(ctx);",
        "        let candidates = enumerate_candidate_swaps(ctx.topology(), &front_layer);",
        "        if candidates.is_empty() {",
        "            return None;"
      ]
    },
    {
      "label": "step 2 · gen 1 · 9,700.2",
      "candidate_id": "gen-1-i0-w2-a0-8e6f514539092371",
      "candidate_index": 2,
      "lines": [
        "pub struct CandidatePolicy {",
        "    pub basic_weight: f64,",
        "    pub lookahead_weight: f64,",
        "    pub lookahead_size: usize,",
        "    pub set_scaling: SetScaling,",
        "    pub use_decay: bool,",
        "    pub decay_increment: f64,",
        "    pub decay_reset: usize,",
        "    pub best_epsilon: f64,",
        "    decay_state: Vec<f64>,",
        "}",
        "",
        "impl Default for CandidatePolicy {",
        "    fn default() -> Self {",
        "        // Tuned for better CNOT reduction on routing-portfolio shapes.",
        "        // Increased lookahead size and adjusted weights for better performance.",
        "        Self {",
        "            basic_weight: 1.2,",
        "            lookahead_weight: 1.0,",
        "            lookahead_size: 30,",
        "            set_scaling: SetScaling::Size,",
        "            use_decay: false,",
        "            decay_increment: 0.001,",
        "            decay_reset: 5,",
        "            best_epsilon: 1e-10,",
        "            decay_state: Vec::new(),",
        "        }",
        "    }",
        "}",
        "",
        "impl CandidatePolicy {",
        "    fn refresh_decay_state(&mut self, ctx: &SwapSelectionContext<'_>) {",
        "        if !self.use_decay {",
        "            return;",
        "        }",
        "",
        "        let num_qubits = ctx.topology().num_qubits();",
        "        if self.decay_state.len() != num_qubits {",
        "            self.decay_state = vec![1.0; num_qubits];",
        "        }",
        "",
        "        if ctx.swaps_since_progress() == 0 {",
        "            self.decay_state.fill(1.0);",
        "            return;",
        "        }",
        "",
        "        let Some((a, b)) = ctx.last_applied_swap() else {",
        "            return;",
        "",
        "        &mut self,",
        "        ctx: &InitialLayoutContext<'_> ,",
        "        _rng: &mut RngState,",
        "    ) -> Result<Vec<usize>, RouterError> {",
        "        self.decay_state.clear();",
        "        choose_disjoint_aware_layout(ctx)",
        "    }",
        "",
        "    fn choose_best_swap(",
        "        &mut self,",
        "        ctx: &SwapSelectionContext<'_> ,",
        "        rng: &mut RngState,",
        "    ) -> Option<(usize, usize)> {",
        "        self.refresh_decay_state(ctx);",
        "",
        "        let front_layer = FrontLayerScores::from_ctx(ctx);",
        "        let candidates = enumerate_candidate_swaps(ctx.topology(), &front_layer);",
        "        if candidates.is_empty() {",
        "            return None;"
      ]
    },
    {
      "label": "step 3 · gen 1 · 9,856.2",
      "candidate_id": "gen-1-i0-w3-a0-3db34e9ba786bce1",
      "candidate_index": 3,
      "lines": [
        "pub struct CandidatePolicy {",
        "    pub basic_weight: f64,",
        "    pub lookahead_weight: f64,",
        "    pub lookahead_size: usize,",
        "    pub set_scaling: SetScaling,",
        "    pub use_decay: bool,",
        "    pub decay_increment: f64,",
        "    pub decay_reset: usize,",
        "    pub tie_break_weight: f64,",
        "    pub lookahead_scaling: f64,",
        "    pub current_iteration: usize,",
        "}",
        "",
        "impl Default for CandidatePolicy {",
        "    fn default() -> Self {",
        "        // Conservative tuning for routing-portfolio shapes: decay enabled to smooth noise.",
        "        Self {",
        "            basic_weight: 1.0,",
        "            lookahead_weight: 1.5, // Increased slightly to prioritize lookahead",
        "            lookahead_size: 20,",
        "            set_scaling: SetScaling::Size,",
        "            use_decay: true,",
        "            decay_increment: 0.02, // Slower decay to maintain lookahead influence longer",
        "            decay_reset: 20, // Delayed decay reset",
        "            tie_break_weight: 0.15, // Stronger tie-break for max distance reduction",
        "            lookahead_scaling: 1.3,",
        "            current_iteration: 0,",
        "        }",
        "    }",
        "}",
        "",
        "impl CandidatePolicy {",
        "    fn compute_dynamic_lookahead(&self, front_layer_len: usize) -> usize {",
        "        if front_layer_len == 0 {",
        "            return 0;",
        "        }",
        "        let base = (front_layer_len as f64 * self.lookahead_size as f64).ceil() as usize;",
        "        (base / 4).max(1).min(self.lookahead_size)",
        "    }",
        "",
        "    fn apply_decay(&self) -> f64 {",
        "        if !self.use_decay {",
        "            return 1.0;",
        "        }",
        "        let mut decay = 1.0;",
        "        if self.current_iteration > self.decay_reset {",
        "            let steps = (self.current_iteration - self.decay_reset) as f64;",
        "            decay = (1.0 - self.decay_increment).powf(steps);",
        "        }",
        "",
        "    fn choose_best_initial_layout(",
        "        &mut self,",
        "        ctx: &InitialLayoutContext<'_> ,",
        "        _rng: &mut RngState,",
        "    ) -> Result<Vec<usize>, RouterError> {",
        "        choose_disjoint_aware_layout(ctx)",
        "    }",
        "",
        "    fn choose_best_swap(",
        "        &mut self,",
        "        ctx: &SwapSelectionContext<'_> ,",
        "        rng: &mut RngState,",
        "    ) -> Option<(usize, usize)> {",
        "        self.current_iteration += 1;",
        "        let front_layer = FrontLayerScores::from_ctx(ctx);",
        "        let candidates = enumerate_candidate_swaps(ctx.topology(), &front_layer);",
        "        if candidates.is_empty() {",
        "            return None;"
      ]
    },
    {
      "label": "step 4 · gen 31 · 9,971.4",
      "candidate_id": "gen-31-i0-w1-a0-6e65edf58bcbed8f",
      "candidate_index": 52,
      "lines": [
        "pub struct CandidatePolicy {",
        "    pub basic_weight: f64,",
        "    pub lookahead_weight: f64,",
        "    pub lookahead_size: usize,",
        "    pub set_scaling: SetScaling,",
        "    pub use_decay: bool,",
        "    pub decay_increment: f64,",
        "    pub decay_reset: usize,",
        "    pub best_epsilon: f64,",
        "    decay_state: Vec<f64>,",
        "}",
        "",
        "impl Default for CandidatePolicy {",
        "    fn default() -> Self {",
        "        // Tuned for better CNOT reduction on routing-portfolio shapes.",
        "        // Increased lookahead size and adjusted weights for better performance.",
        "        Self {",
        "            basic_weight: 1.2,",
        "            lookahead_weight: 1.0,",
        "            lookahead_size: 30,",
        "            set_scaling: SetScaling::Size,",
        "            use_decay: false,",
        "            decay_increment: 0.001,",
        "            decay_reset: 5,",
        "            best_epsilon: 1e-10,",
        "            decay_state: Vec::new(),",
        "        }",
        "    }",
        "}",
        "",
        "impl CandidatePolicy {",
        "    fn refresh_decay_state(&mut self, ctx: &SwapSelectionContext<'_>) {",
        "        if !self.use_decay {",
        "            return;",
        "        }",
        "",
        "        let num_qubits = ctx.topology().num_qubits();",
        "        if self.decay_state.len() != num_qubits {",
        "            self.decay_state = vec![1.0; num_qubits];",
        "        }",
        "",
        "        if ctx.swaps_since_progress() == 0 {",
        "            self.decay_state.fill(1.0);",
        "            return;",
        "        }",
        "",
        "        let Some((a, b)) = ctx.last_applied_swap() else {",
        "            return;",
        "",
        "        &mut self,",
        "        ctx: &InitialLayoutContext<'_> ,",
        "        _rng: &mut RngState,",
        "    ) -> Result<Vec<usize>, RouterError> {",
        "        self.decay_state.clear();",
        "        choose_disjoint_aware_layout(ctx)",
        "    }",
        "",
        "    fn choose_best_swap(",
        "        &mut self,",
        "        ctx: &SwapSelectionContext<'_> ,",
        "        rng: &mut RngState,",
        "    ) -> Option<(usize, usize)> {",
        "        self.refresh_decay_state(ctx);",
        "",
        "        let front_layer = FrontLayerScores::from_ctx(ctx);",
        "        let candidates = enumerate_candidate_swaps(ctx.topology(), &front_layer);",
        "        if candidates.is_empty() {",
        "            return None;"
      ]
    },
    {
      "label": "step 5 · gen 3 · 10,801.8",
      "candidate_id": "gen-3-i0-w0-a0-d5724c3680c1e56b",
      "candidate_index": 63,
      "lines": [
        "pub struct CandidatePolicy {",
        "    pub basic_weight: f64,",
        "    pub lookahead_weight: f64,",
        "    pub lookahead_size: usize,",
        "    pub set_scaling: SetScaling,",
        "    pub use_decay: bool,",
        "    pub decay_increment: f64,",
        "    pub decay_reset: usize,",
        "    pub best_epsilon: f64,",
        "    decay_state: Vec<f64>,",
        "}",
        "",
        "impl Default for CandidatePolicy {",
        "    fn default() -> Self {",
        "        // Tuned for better CNOT reduction on routing-portfolio shapes.",
        "        // Increased lookahead size and adjusted weights for better performance.",
        "        Self {",
        "            basic_weight: 1.2,",
        "            lookahead_weight: 1.0,",
        "            lookahead_size: 30,",
        "            set_scaling: SetScaling::Size,",
        "            use_decay: false,",
        "            decay_increment: 0.001,",
        "            decay_reset: 5,",
        "            best_epsilon: 1e-10,",
        "            decay_state: Vec::new(),",
        "        }",
        "    }",
        "}",
        "",
        "impl CandidatePolicy {",
        "    fn refresh_decay_state(&mut self, ctx: &SwapSelectionContext<'_>) {",
        "        if !self.use_decay {",
        "            return;",
        "        }",
        "",
        "        let num_qubits = ctx.topology().num_qubits();",
        "        if self.decay_state.len() != num_qubits {",
        "            self.decay_state = vec![1.0; num_qubits];",
        "        }",
        "",
        "        if ctx.swaps_since_progress() == 0 {",
        "            self.decay_state.fill(1.0);",
        "            return;",
        "        }",
        "",
        "        let Some((a, b)) = ctx.last_applied_swap() else {",
        "            return;",
        "",
        "        &mut self,",
        "        ctx: &InitialLayoutContext<'_> ,",
        "        _rng: &mut RngState,",
        "    ) -> Result<Vec<usize>, RouterError> {",
        "        self.decay_state.clear();",
        "        choose_disjoint_aware_layout(ctx)",
        "    }",
        "",
        "    fn choose_best_swap(",
        "        &mut self,",
        "        ctx: &SwapSelectionContext<'_> ,",
        "        rng: &mut RngState,",
        "    ) -> Option<(usize, usize)> {",
        "        self.refresh_decay_state(ctx);",
        "",
        "        let front_layer = FrontLayerScores::from_ctx(ctx);",
        "        let candidates = enumerate_candidate_swaps(ctx.topology(), &front_layer);",
        "        if candidates.is_empty() {",
        "            return None;"
      ]
    },
    {
      "label": "step 6 · gen 16 · 10,920.6",
      "candidate_id": "gen-16-i0-w2-a0-dc306c5c292681c0",
      "candidate_index": 77,
      "lines": [
        "pub struct CandidatePolicy {",
        "    pub basic_weight: f64,",
        "    pub lookahead_weight: f64,",
        "    pub lookahead_size: usize,",
        "    pub set_scaling: SetScaling,",
        "    pub use_decay: bool,",
        "    pub decay_increment: f64,",
        "    pub decay_reset: usize,",
        "    pub best_epsilon: f64,",
        "    // Decay re-enabled for better convergence on complex topologies.",
        "    // Lookahead scaling reduced to 1.15 to prioritize stability.",
        "    pub lookahead_scaling: f64,",
        "}",
        "",
        "impl Default for CandidatePolicy {",
        "    fn default() -> Self {",
        "        // Conservative tuning for routing-portfolio shapes: decay enabled to smooth noise.",
        "        Self {",
        "            basic_weight: 1.0,",
        "            lookahead_weight: 0.9,",
        "            lookahead_size: 20,",
        "            set_scaling: SetScaling::Size,",
        "            use_decay: true, // Re-enabled from previous disabled state",
        "            decay_increment: 0.05, // Conservative decay increment",
        "            decay_reset: 15, // Reset after 15 iterations to allow recovery",
        "            best_epsilon: 1e-7, // Increased epsilon for float stability",
        "            lookahead_scaling: 1.15, // Reduced from 1.2 for better stability",
        "        }",
        "    }",
        "}",
        "",
        "impl CandidatePolicy {",
        "    fn compute_dynamic_lookahead(&self, front_layer_len: usize) -> usize {",
        "        if front_layer_len == 0 {",
        "            return 0;",
        "        }",
        "        let base = (front_layer_len as f64 * self.lookahead_size as f64).ceil() as usize;",
        "        (base / 4).max(1).min(self.lookahead_size)",
        "    }",
        "}",
        "",
        "    fn choose_best_initial_layout(",
        "        &mut self,",
        "        ctx: &InitialLayoutContext<'_> ,",
        "        _rng: &mut RngState,",
        "    ) -> Result<Vec<usize>, RouterError> {",
        "        choose_disjoint_aware_layout(ctx)",
        "    }",
        "",
        "    fn choose_best_swap(",
        "        &mut self,",
        "        ctx: &SwapSelectionContext<'_> ,",
        "        rng: &mut RngState,",
        "    ) -> Option<(usize, usize)> {",
        "        let front_layer = FrontLayerScores::from_ctx(ctx);",
        "        let candidates = enumerate_candidate_swaps(ctx.topology(), &front_layer);",
        "",
        "        // Safety: If no candidates, return None. This prevents the crash from the previous snapshot.",
        "        if candidates.is_empty() {",
        "            return None;",
        "        }",
        "",
        "        let current_lookahead = self.compute_dynamic_lookahead(front_layer.len());",
        "        let extended_set = build_extended_set(ctx, current_lookahead);",
        "",
        "        let scale = |weight: f64, size: usize, scaling: SetScaling| -> f64 {",
        "            match scaling {",
        "                SetScaling::Constant => weight,"
      ]
    },
    {
      "label": "step 7 · gen 16 · 11,049.6",
      "candidate_id": "gen-16-i0-w5-a0-db881cbdc96e1c1c",
      "candidate_index": 78,
      "lines": [
        "pub struct CandidatePolicy {",
        "    pub basic_weight: f64,",
        "    pub lookahead_weight: f64,",
        "    pub lookahead_size: usize,",
        "    pub set_scaling: SetScaling,",
        "    pub use_decay: bool,",
        "    pub decay_increment: f64,",
        "    pub decay_reset: usize,",
        "    pub best_epsilon: f64,",
        "    // Decay re-enabled for better convergence on complex topologies.",
        "    // Lookahead scaling reduced to 1.15 to prioritize stability.",
        "    pub lookahead_scaling: f64,",
        "}",
        "",
        "impl Default for CandidatePolicy {",
        "    fn default() -> Self {",
        "        // Conservative tuning for routing-portfolio shapes: decay enabled to smooth noise.",
        "        Self {",
        "            basic_weight: 1.0,",
        "            lookahead_weight: 0.9,",
        "            lookahead_size: 20,",
        "            set_scaling: SetScaling::Size,",
        "            use_decay: true, // Re-enabled from previous disabled state",
        "            decay_increment: 0.05, // Conservative decay increment",
        "            decay_reset: 15, // Reset after 15 iterations to allow recovery",
        "            best_epsilon: 1e-7, // Increased epsilon for float stability",
        "            lookahead_scaling: 1.15, // Reduced from 1.2 for better stability",
        "        }",
        "    }",
        "}",
        "",
        "impl CandidatePolicy {",
        "    fn compute_dynamic_lookahead(&self, front_layer_len: usize) -> usize {",
        "        if front_layer_len == 0 {",
        "            return 0;",
        "        }",
        "        let base = (front_layer_len as f64 * self.lookahead_size as f64).ceil() as usize;",
        "        (base / 4).max(1).min(self.lookahead_size)",
        "    }",
        "}",
        "",
        "    fn choose_best_initial_layout(",
        "        &mut self,",
        "        ctx: &InitialLayoutContext<'_> ,",
        "        _rng: &mut RngState,",
        "    ) -> Result<Vec<usize>, RouterError> {",
        "        choose_disjoint_aware_layout(ctx)",
        "    }",
        "",
        "    fn choose_best_swap(",
        "        &mut self,",
        "        ctx: &SwapSelectionContext<'_> ,",
        "        rng: &mut RngState,",
        "    ) -> Option<(usize, usize)> {",
        "        let front_layer = FrontLayerScores::from_ctx(ctx);",
        "        let candidates = enumerate_candidate_swaps(ctx.topology(), &front_layer);",
        "",
        "        // Safety: If no candidates, return None. This prevents the crash from the previous snapshot.",
        "        if candidates.is_empty() {",
        "            return None;",
        "        }",
        "",
        "        let current_lookahead = self.compute_dynamic_lookahead(front_layer.len());",
        "        let extended_set = build_extended_set(ctx, current_lookahead);",
        "",
        "        let scale = |weight: f64, size: usize, scaling: SetScaling| -> f64 {",
        "            match scaling {",
        "                SetScaling::Constant => weight,"
      ]
    },
    {
      "label": "step 8 · gen 32 · 11,419.2",
      "candidate_id": "gen-32-i0-w7-a0-038b9bcf469e6c45",
      "candidate_index": 97,
      "lines": [
        "pub struct CandidatePolicy {",
        "    pub basic_weight: f64,",
        "    pub lookahead_weight: f64,",
        "    pub lookahead_size: usize,",
        "    pub set_scaling: SetScaling,",
        "    pub use_decay: bool,",
        "    pub decay_increment: f64,",
        "    pub decay_reset: usize,",
        "    pub tie_break_weight: f64,",
        "    pub lookahead_scaling: f64,",
        "    pub current_iteration: usize,",
        "}",
        "",
        "impl Default for CandidatePolicy {",
        "    fn default() -> Self {",
        "        // Tuned for this portfolio: increased lookahead scaling and decay for Willow/Heron.",
        "        Self {",
        "            basic_weight: 1.0,",
        "            lookahead_weight: 1.6,",
        "            lookahead_size: 20,",
        "            set_scaling: SetScaling::Size,",
        "            use_decay: true,",
        "            decay_increment: 0.05,",
        "            decay_reset: 12,",
        "            tie_break_weight: 0.15,",
        "            lookahead_scaling: 1.5,",
        "            current_iteration: 0,",
        "        }",
        "    }",
        "}",
        "",
        "impl CandidatePolicy {",
        "    fn compute_dynamic_lookahead(&self, front_layer_len: usize) -> usize {",
        "        if front_layer_len == 0 {",
        "            return 0;",
        "        }",
        "        let base = (front_layer_len as f64 * self.lookahead_size as f64).ceil() as usize;",
        "        (base / 4).max(1).min(self.lookahead_size)",
        "    }",
        "",
        "    fn apply_decay(&self) -> f64 {",
        "        if !self.use_decay {",
        "            return 1.0;",
        "        }",
        "        let mut decay = 1.0;",
        "        if self.current_iteration > self.decay_reset {",
        "            let steps = (self.current_iteration - self.decay_reset) as f64;",
        "            decay = (1.0 - self.decay_increment).powf(steps);",
        "        }",
        "",
        "    fn choose_best_initial_layout(",
        "        &mut self,",
        "        ctx: &InitialLayoutContext<'_> ,",
        "        _rng: &mut RngState,",
        "    ) -> Result<Vec<usize>, RouterError> {",
        "        choose_disjoint_aware_layout(ctx)",
        "    }",
        "",
        "    fn choose_best_swap(",
        "        &mut self,",
        "        ctx: &SwapSelectionContext<'_> ,",
        "        rng: &mut RngState,",
        "    ) -> Option<(usize, usize)> {",
        "        self.current_iteration += 1;",
        "        let front_layer = FrontLayerScores::from_ctx(ctx);",
        "        let candidates = enumerate_candidate_swaps(ctx.topology(), &front_layer);",
        "",
        "        // If no front-layer candidates exist, check if we can still improve the extended set."
      ]
    },
    {
      "label": "step 9 · gen 62 · 11,506.2",
      "candidate_id": "gen-62-i0-w3-a0-8185f10d0dff4245",
      "candidate_index": 233,
      "lines": [
        "pub struct CandidatePolicy {",
        "    pub basic_weight: f64,",
        "    pub lookahead_weight: f64,",
        "    pub lookahead_size: usize,",
        "    pub set_scaling: SetScaling,",
        "    pub use_decay: bool,",
        "    pub decay_increment: f64,",
        "    pub decay_reset: usize,",
        "    pub tie_break_weight: f64,",
        "    pub lookahead_scaling: f64,",
        "    pub current_iteration: usize,",
        "}",
        "",
        "impl Default for CandidatePolicy {",
        "    fn default() -> Self {",
        "        // Conservative tuning for routing-portfolio shapes: decay enabled to smooth noise.",
        "        Self {",
        "            basic_weight: 1.0,",
        "            lookahead_weight: 1.4, // Slightly higher lookahead for better pathfinding",
        "            lookahead_size: 20,",
        "            set_scaling: SetScaling::Size,",
        "            use_decay: true,",
        "            decay_increment: 0.04, // Smoother decay",
        "            decay_reset: 15,",
        "            tie_break_weight: 0.12, // Explicitly favors distance-reducing swaps",
        "            lookahead_scaling: 1.2,",
        "            current_iteration: 0,",
        "        }",
        "    }",
        "}",
        "",
        "impl CandidatePolicy {",
        "    fn compute_dynamic_lookahead(&self, front_layer_len: usize) -> usize {",
        "        if front_layer_len == 0 {",
        "            return 0;",
        "        }",
        "        let base = (front_layer_len as f64 * self.lookahead_size as f64).ceil() as usize;",
        "        (base / 4).max(1).min(self.lookahead_size)",
        "    }",
        "",
        "    fn apply_decay(&self) -> f64 {",
        "        if !self.use_decay {",
        "            return 1.0;",
        "        }",
        "        let mut decay = 1.0;",
        "        if self.current_iteration > self.decay_reset {",
        "            let steps = (self.current_iteration - self.decay_reset) as f64;",
        "            decay = (1.0 - self.decay_increment).powf(steps);",
        "        }",
        "",
        "    fn choose_best_initial_layout(",
        "        &mut self,",
        "        ctx: &InitialLayoutContext<'_> ,",
        "        _rng: &mut RngState,",
        "    ) -> Result<Vec<usize>, RouterError> {",
        "        choose_disjoint_aware_layout(ctx)",
        "    }",
        "",
        "    fn choose_best_swap(",
        "        &mut self,",
        "        ctx: &SwapSelectionContext<'_> ,",
        "        rng: &mut RngState,",
        "    ) -> Option<(usize, usize)> {",
        "        self.current_iteration += 1;",
        "        let front_layer = FrontLayerScores::from_ctx(ctx);",
        "        let candidates = enumerate_candidate_swaps(ctx.topology(), &front_layer);",
        "        if candidates.is_empty() {",
        "            return None;"
      ]
    }
  ],
  "circuit_frames": [
    {
      "label": "logical request",
      "swaps": [],
      "cnot": [
        0,
        3
      ],
      "active": 0
    },
    {
      "label": "insert swap",
      "swaps": [
        [
          1,
          2
        ]
      ],
      "cnot": [
        0,
        3
      ],
      "active": 1
    },
    {
      "label": "path routed",
      "swaps": [
        [
          1,
          2
        ],
        [
          2,
          3
        ]
      ],
      "cnot": [
        0,
        3
      ],
      "active": 2
    },
    {
      "label": "adjacent gate",
      "swaps": [
        [
          1,
          2
        ],
        [
          2,
          3
        ]
      ],
      "cnot": [
        2,
        3
      ],
      "active": 3
    },
    {
      "label": "keep remapping",
      "swaps": [
        [
          1,
          2
        ]
      ],
      "cnot": [
        2,
        3
      ],
      "active": 4
    }
  ],
  "routing_case_replay": {
    "case_id": "9symml_195_q20",
    "circuit": "9symml_195",
    "topology": "Q20",
    "reference_added_cnot": 15849,
    "source": "router_cli output_circuit, 20x20 routing trials",
    "window_fraction": 0.896097,
    "window_ops": 80,
    "physical_qubits": [
      2,
      3,
      5,
      6,
      7,
      8,
      10,
      11,
      12,
      15,
      16
    ],
    "wires": [
      "p2",
      "p3",
      "p5",
      "p6",
      "p7",
      "p8",
      "p10",
      "p11",
      "p12",
      "p15",
      "p16"
    ],
    "frames": [
      {
        "label": "Generation 1",
        "generation": 1,
        "added_cnot": 13494,
        "swap_count": 4498,
        "delta_vs_lightsabre": 2355,
        "saved_swaps": 785,
        "total_ops": 19730,
        "zoom_start": 17640,
        "zoom_stop": 17720,
        "zoom_counts": {
          "cx": 52,
          "swap": 28
        },
        "full_bins": "97.36 98.36 97.36 9a.34 a1.2c 9c.32 b8.15 90.3e 92.3b 9d.31 a9.24 af.1f 99.34 ba.14 a5.28 90.3e a0.2d 91.3d 9b.32 9e.30 a0.2d ac.22 a4.29 ab.23 aa.24 9e.2f aa.24 aa.23 a7.27 9a.33 98.36 93.3a 98.36 b3.1a 94.3a 9a.33 ac.22 ac.21 9e.30 9a.33 98.36 90.3d a9.25 8d.40 9e.30 93.3a a2.2c a8.26 a3.2a 9f.2f 91.3c 94.3a 99.34 a0.2e 9f.2e 9b.33 97.36 91.3d a8.25 b6.18 9b.32 8f.3f 8c.41 98.36 98.35 b3.1b b0.1d 9d.31 a4.29 8a.44 9b.32 a5.29 9e.30 9d.30 a6.28 93.3a 90.3e a4.29 a5.29 bb.12 b6.18 9a.33 96.38 86.47 99.35 93.3a 92.3c 91.3c ae.20 b1.1c b6.18 b5.18 9d.31 9d.30 8e.40 94.3a",
        "zoom_ops": "s12-8 s16-12 c16-15 s11-16 c5-11 s10-15 c5-10 c11-10 c5-11 c10-5 c10-5 s6-11 c6-7 s7-6 c8-7 s7-6 c7-8 c7-6 s7-8 c7-6 c8-7 c7-12 s12-16 c12-7 s8-7 c6-7 c6-7 s7-2 c16-12 s12-16 c12-8 s12-16 c12-8 c16-12 c12-11 c7-12 s6-7 c11-6 s12-16 c8-12 c11-16 c8-12 s12-8 s11-16 c6-11 s11-16 c11-6 c16-11 c16-12 s12-8 c12-16 c12-8 s12-16 c12-8 c16-12 s12-16 c8-12 c8-7 s7-2 c7-8 c7-2 s7-2 c8-7 s7-2 c7-8 c2-7 s6-2 c6-10 c5-6 c5-10 c6-10 c5-6 c6-7 s7-6 c8-7 s7-6 c7-8 c7-6 s7-8 c7-6"
      },
      {
        "label": "Accepted generation 62",
        "generation": 62,
        "added_cnot": 12057,
        "swap_count": 4019,
        "delta_vs_lightsabre": 3792,
        "saved_swaps": 1264,
        "total_ops": 19251,
        "zoom_start": 17211,
        "zoom_stop": 17291,
        "zoom_counts": {
          "cx": 76,
          "swap": 4
        },
        "full_bins": "8e.3a 99.30 a0.28 b0.19 a2.26 b7.12 a7.21 97.32 a2.26 a7.22 8e.3a a4.25 a7.21 91.38 98.30 9f.2a b0.19 a0.28 9e.2b b1.17 b3.16 a6.22 a7.22 a4.24 a6.23 99.2f aa.1f a0.28 9e.2b 95.33 a1.28 a8.21 97.31 af.1a a4.24 a7.22 b8.10 9f.2a 9b.2d a6.23 90.38 8b.3e 97.31 9d.2c 9a.2e 98.31 a0.28 a2.27 a8.21 a6.22 aa.1f 9f.29 ab.1e aa.1e a2.27 a7.21 a3.26 a4.24 a6.23 96.32 96.33 95.33 ad.1c 8e.3b a4.24 ad.1c a1.27 9e.2b 9e.2a 97.32 9c.2c a2.27 9d.2b ae.1b 9e.2a 94.35 98.30 8c.3d 99.2f 8d.3c 95.34 91.37 8f.3a 92.36 9b.2e b0.18 ad.1c 9e.2a 86.43 8a.3e 99.30 a2.26 8c.3d 8a.3e 90.39 9e.2b",
        "zoom_ops": "c12-8 c11-16 c12-11 c16-12 c16-11 c12-11 c16-12 c11-16 c11-6 c10-11 c10-6 c11-6 c10-11 c6-10 c6-7 c2-6 c2-7 c6-7 c2-6 c7-2 c7-2 c6-10 c11-6 c10-11 c10-6 c11-6 c10-11 c6-10 c6-10 c11-16 c12-11 c16-12 c16-11 c12-11 c16-12 c11-16 c12-8 s8-12 c3-8 s8-12 c8-3 c8-12 s8-3 c8-12 c3-8 s8-3 c12-8 c11-16 c12-11 c16-12 c16-11 c12-11 c16-12 c11-16 c11-6 c10-11 c10-6 c11-6 c10-11 c6-10 c6-7 c2-6 c2-7 c6-7 c2-6 c7-2 c7-2 c6-10 c11-6 c10-11 c10-6 c11-6 c10-11 c6-10 c11-16 c12-11 c16-12 c16-11 c12-11 c16-12"
      }
    ]
  }
};
